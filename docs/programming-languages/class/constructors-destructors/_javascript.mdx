```Javascript
class MyClass {
	constructor(value) {
		this.value = value;
	}
}

const myClass = new MyClass(10);

// Destructors are Not Available.
// Use explicit cleanup methods when needed.
class Resource {
	close() {
		// cleanup
	}
}
```
