```Java
enum Color {
	RED,
	GREEN,
	BLUE
}

enum HttpStatus {
	OK(200),
	NOT_FOUND(404);

	private final int code;

	HttpStatus(int code) {
		this.code = code;
	}

	public int getCode() {
		return code;
	}
}

Color color = Color.RED;
String name = color.name();
```
