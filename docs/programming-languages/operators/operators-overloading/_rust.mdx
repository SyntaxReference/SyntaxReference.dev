```Rust
use std::ops::Add;

struct Vector2 {
	x: f32,
	y: f32,
}

impl Add for Vector2 {
	type Output = Vector2;

	fn add(self, other: Vector2) -> Vector2 {
		Vector2 {
			x: self.x + other.x,
			y: self.y + other.y,
		}
	}
}

let result = first + second;
```
